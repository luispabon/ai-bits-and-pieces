interface:
  display_name: "Coding Loop Planner"
  short_description: "Plan coding work with research and staged plans."
  default_prompt: "Create a coding plan with a high-level overview first, optional research, and a staged implementation manifest under .project_planning. Keep all writes confined to that directory."
